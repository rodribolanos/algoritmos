#include <iostream>
#include <vector>

using namespace std;

int main() {
    long r, n, c, m;

    while (cin >> r >> n >> c >> m && r != 0 && n != 0 && c != 0 && m != 0) {

        vector<vector<bool>> rectangular(r, vector<bool>(n, false));
        
    }
}
// programa sobre como pedir un input "infinito" (corta cuando el usuario le da Ctrl C), no tiene cantidad de entrada
#include <iostream>

int main() {
    int entrada;
    while (cin >> entrada) {
        
        cout << hacerAlgo() << "\n"
        
    }
}